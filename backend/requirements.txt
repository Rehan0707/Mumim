# Munim.ai backend — CORE runtime deps (lightweight, no ML weights).
# The app runs fully on these (mock/hash mode); real ML is opt-in via requirements-ml.txt.
# All heavy-ML imports in the code are lazy, so nothing here pulls torch.
# Lower-bound pins so wheels resolve cleanly across Python 3.9–3.13+.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.0
pgvector>=0.2.0
pydantic>=2.8.0
python-multipart>=0.0.9
numpy>=1.26.0
